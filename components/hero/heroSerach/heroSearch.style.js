import styled from "styled-components";

export const DatePickerWrapper = styled.div`
  .heroContent {
    &__input--datepicker {
      border: none;
      font-weight: 700;
      font-size: 1.125rem;
      padding: 0px;
      text-align: left;
    }
  }
`;
